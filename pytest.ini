[pytest]
testpaths = tests
asyncio_mode = auto
markers =
    unit: Pure unit tests (no LLM, no network)
    integration: API-level tests with mocked LLM backend
    correctness: Output quality and structural correctness tests
    negative: Negative-path / access-control enforcement tests
    adversarial: Boundary, malformed-input, and adversarial-condition tests
